<form method="post" action="{{ path('render_delete', {'id': render.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ render.id) }}">
    <button class="btn">Delete</button>
</form>
